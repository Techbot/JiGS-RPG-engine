# node.type.map_grid.yml
langcode: en
status: true
dependencies:
  enforced:
    module:
      - jigs # This is the name of the module we're using for this example
name: 'Map-Grid'
type: map_grid
description: 'Content type that can be used to collate objects for <em>Map-Grids</em>'
help: ''
new_revision: false
preview_mode: 1
display_submitted: true
